Layout/bike
===========

**simple layout for bike. no dashed lines - optimised for broken winCE
renderer**

The ``Bike`` layout is shipped with Navit and can be selected from the
GUI (Internal GUI -> Settings -> Display -> Layout). Its source is
``navit_layout_bike_shipped.xml`` in the repository, installed as
``navit_layout_bike.xml``.